---
title: ZhipuAI
---

The `ZhipuAIEmbeddings` class uses the ZhipuAI API to generate embeddings for a given text.

## Setup

You'll need to sign up for an ZhipuAI API key and set it as an environment variable named `ZHIPUAI_API_KEY`.

https://open.bigmodel.cn

Then, you'll need to install the [`@langchain/community`](https://www.npmjs.com/package/@langchain/community) package:

import IntegrationInstallTooltip from '/snippets/javascript-integrations/integration-install-tooltip.mdx';

<IntegrationInstallTooltip/>

```bash npm
npm install @langchain/community @langchain/core jsonwebtoken
```

## Usage

import Zhipuai from "/snippets/javascript-integrations/examples/embeddings/zhipuai.mdx";

<Zhipuai />

## Related

- Embedding model [conceptual guide](/oss/concepts/embedding_models)
- Embedding model [how-to guides](/oss/how-to/#embedding-models)
